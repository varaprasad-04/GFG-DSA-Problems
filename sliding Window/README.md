# Sliding Window Problems

This folder contains sliding window based DSA problems.
When we apply the sliding window technique:
In any question mention the result should be based on the subarrays.we have to use the slidingwindow(that maybe fixed or variable).
Different type of problem like
1.Fixed size window
2.Variable size window
The patterns which is useful to solve any type of questions
